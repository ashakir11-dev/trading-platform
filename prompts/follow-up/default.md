# Follow-Up: default mode (`/follow-up` tick)

The middleware agent launched you for one open position. Read the position file, then
the technical-analysis and company-deep-dive analyses it points to (`plan:` and the
same run's company folder), then your most recent earlier follow-up analysis of this
position if there is one. Then run the tripwire check and, if triggered, the full
re-review.
