# Technical Analysis: default mode (end-to-end run)

Your brief names one candidate that passed the company deep dive: the ticker, its
direction, the company deep dive's folder as `upstream`, and the run's profile file.

1. Read the company deep dive's `output.md`: the thesis, catalyst checks, risks and
   `next_earnings`. Use it as context for event risk, not as a reason to force a plan.
2. Build the plan from the chart alone, then run every rule in your role.
